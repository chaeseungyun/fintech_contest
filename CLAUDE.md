# FinStay AI — 보유 금융상품 상시 분석 에이전트 (핀테크 공모전 데모)

Vite + React + TypeScript 정적 웹 앱. 서버·API·LLM 호출 없음. 상태는 React state만.
유일한 데이터 소스는 `src/fixtures/scenario.json`. 오늘 날짜는 `meta.today`.

모바일 금융앱 UI다. 데스크톱에서는 폰 프레임 하나가 가운데 놓이고, 좁은 화면에서는 전체를 채운다.

서비스명은 `scenario.json`의 `brand.service` 하나에서만 온다. 화면·메뉴·타이틀 어디에도 이름을 적지 않는다.
이 기능은 **은행 앱 안의 메뉴**다 — 홈 상단은 `brand.bank`(은행), 내부 배너·허브 제목만 `brand.service`다. 독립 앱처럼 보이게 하지 않는다.
홈에는 "샘플 데이터 · 기준일" 칩이 항상 붙는다. 처음 보는 사람이 데모임을 설명 없이 알아야 한다.

**이 서비스는 갈아타기 추천 서비스가 아니다.** 보유 상품 사이의 연결과 손익 변화를 상시 분석해
유지·변경·추가 가입의 의사결정을 돕는다. 흐름은 `상시 분석 → 손익 판정 → 최종 판단 → 실행 안내`이고,
대체·연계 상품 제안은 그 판정을 통과한 경우에만 따라붙는 결과다.

- `npm run dev` 개발 서버 · `npm test` 테스트 · `npm run build` 정적 빌드(`dist/`)
- 계산 로직은 `src/lib/` 순수 함수. 화면은 `src/screens/`, 단일 상태는 `src/state/store.ts`.

## 화면 구조

하단 탭 4개(`홈·자산·혜택·전체`) 위에 분석 흐름이 스택으로 쌓인다. `store.ts`의 `{ tab, stack }` 하나가 전부다.

```
홈 ─(AI 배너·퀵메뉴)─▶ hub ─▶ analyzing ─▶ verdict ─▶ impact ─▶ actionplan
홈 ─(상시 점검 카드 행)─▶ 혜택 탭                        │            │
혜택 탭 ─(손익 분석하기)─┘                  connections ◀┤            │
                                            timeline ◀───┴────────────┘
                                            evidence ◀─(영향 항목·관계도 선의 근거 보기)
```

- `hub`가 유일한 "항목 고르기" 화면이다. 트리거 목록을 다른 탭에 복제하지 않는다.
  허브 행에는 **손익 금액을 미리 적지 않는다** — 연결 건수·영향 상품·확인 필요 입력만. 숫자는 분석을 거친 뒤 나온다.
  예외 하나: 혜택 탭의 조건 행에서 그 조건이 걸린 상품의 분석(`watch.rows[].triggerId`)으로 바로 가는 "분석 보기"는 목록 복제가 아니라 맥락 링크라 둔다.
- 홈의 "이번 달 점검" 행은 혜택 탭(상시 점검)으로 간다. 점검을 눌렀는데 해지 분석으로 튀지 않는다.
- **결론 먼저.** 분석이 끝나면 `verdict`(비교 결과)가 먼저 뜬다 — 결론 한 줄·순손익 하나·기간별 차트까지만, 스크롤 없이 끝나야 한다
  (팀 피드백 2026-09-20: 처음 보는 사람이 결론 전에 항목 행을 읽게 하지 않는다). "분석 과정 보기"로 `impact`(결과 상세)에 들어간다.
  화면 이름은 "최종 판단"이 아니라 **"비교 결과"**다. 헤드라인은 "확인된 조건에서는"으로 한정한다.
  같은 값을 두 화면에 크게 다시 그리지 않는다 — `impact`의 합계 카드는 "절감 − 손실" 근거 줄이다.
  중도해지 이자는 `impact`와 `actionplan`에만, 절감액은 `impact`의 항목 행과 합계 근거에만 있다.
- `impact`는 **① 상품 자체 변화(절감) ② 연결된 상품 영향 ③ 변경 비용(일회성)** 세 묶음 뒤에
  유지 조건(`maintain`)·갈아타기 후보(`RecommendCard`)·체크리스트가 온다. 절차의 기준 안은 여기서 고르고, 실행 안내로 가는 버튼도 여기 있다.
  항목 행을 펼치면 첫 줄이 쉬운 말 한 문장이다("톡톡카드를 해지하면 주담대의 우대금리 0.2%p가 빠져 대출 이자가 연 12만원 늘어납니다") — 값은 계산 결과에서만 조립한다.
- `hub`는 두 층이다. 상단은 **상시 분석의 증거**(`watch` — 상품 n개·조건 m건을 계속 보고 있음, 이번 달 확인일, 실적 미달; 홈 배너와 같은 값),
  아래는 **"바꾸면 어떻게 될지 미리 보기"**(트리거 목록). "카드를 해지하면?"이라는 가정은 사용자만 줄 수 있으니 트리거는 상시가 아니다 —
  "분석하고 있다"고 해 놓고 고르라고 하는 어긋남을 이 두 층으로 푼다. 목록 위 3단계 칩(손익 비교 → 시점 → 실행 순서)은 누르면 진행되는 순서다.
  `analyzing` 제목도 "미리 찾아 둔 연결로 손익을 계산하고 있습니다" — 처음 분석하는 것처럼 쓰지 않는다.
- 화면 문구에서 내부 용어를 쓰지 않는다 (팀 피드백 2026-09-20). 코드의 `judgment`·`safeAfter`는 그대로 두되 사용자에게는
  "판정일" → **우대 확인일**, "판정 주기" → **확인 주기**, "안전 시점" → **변경 가능 구간**이다. "끝난다"류 문장에는 결과까지 붙인다
  ("…우대 확인이 끝나 이번 달 우대가 확정됩니다. 그 뒤에 바꿔도 이번 달 혜택은 잃지 않습니다"). 정의 한 줄은 혜택 탭 각주와 타임라인에만 둔다.
  되돌릴 수 없는 우대는 상품·우대폭·만기·결과를 다 적는다 — "우대의 만기" 같은 줄임말을 쓰지 않는다.
- 금액 가까이에는 항상 **비교 기준**(`BasisStrip` — 무엇 vs 무엇 · 기간 · 기준일 · 가정)이 붙는다. 값은 `derive().basis`에서만 온다.
- 눌러서 아무 일도 없는 버튼(검색·알림·안내)을 두지 않는다. 시안 껍데기는 들이지 않는다.

- `홈`의 상시 점검 카드와 `혜택` 탭이 상시 분석 표면이다. 둘 다 `watch.ts`의 같은 결과를 읽는다 — 화면이 각자 세지 않는다.
- `analyzing` 은 연출이지만 숫자는 만들지 않는다. `derive()` 가 이미 계산해 둔 `steps` 를 단계별로 드러낼 뿐이다.
  **약관을 방금 읽은 것처럼 보이게 하지 않는다.** 첫 단계는 "추출해 둔 조건 불러오기"이고 화면에 "샘플 조건 · 실시간 추출·AI 호출 없음"을 적는다.
- `actionplan`(실행 안내)은 판단 다음 화면이다. **해지·가입을 대신 실행하지 않는다.** 순서·기한·창구·확인할 질문만 보여준다.
  기준 안(대체 상품)은 **사용자가 결과 상세에서 고른 것**(`store.chosen` → `derive(…, { chosenCandidateId })`)이다.
  고르지 않았으면 "새 상품 없이"다. 앱이 고른 `best`로 절차를 짜지 않는다.
- `timeline`(안전 시점) · `connections`(관계도) · `evidence`(약관 원문·값 수정) 는 메인 흐름에서 한 단계 들어간 상세 화면이다. 없애지 않는다 — 이 프로젝트가 다른 데모와 갈리는 지점이다.

## 계산 규칙 — 여기가 이 프로젝트의 본체다

**날짜 (`dates.ts`)**
- 타임존 `Asia/Seoul` 고정. 시각 없이 날짜만 다룬다. `Date` 객체의 시분초를 절대 쓰지 않는다.
- 구간은 반열림 `[start, end)`로 통일한다.
- 월 오프셋은 말일 clamping을 한다. 1/31에 `+1M`은 2/28(29)이다.
- 년·월·일 오프셋이 섞이면 년 → 월 → 일 순서로 적용한다. 순서를 바꾸면 결과가 달라진다.
- 오늘은 `scenario.json`의 `meta.today`를 쓴다. **`new Date()`를 호출하지 않는다.** 발표 당일 타임라인이 깨진다.

**연산자 평가 (`interpreter.ts`)**

각 조건의 `expr.op`에 따라 "다음 판정일"과 "회복 가능 여부"를 낸다.

- `RECUR` — `params.from.dayOfMonth` 기준으로 오늘 이후 첫 판정일. 오늘이 그 날이면 오늘로 본다.
- `ROLLING` — `params.settleOn`이 `month_end`면 이번 달 말일.
- `COUNT` — `params.checkOn.dayOfMonth` 기준 다음 확인일.
- `PERMANENT` — 판정일 없음(`null`). 회복 불가로 표시하고 `params.until` 앵커를 상품의 `facts.maturity`로 바인딩해 회복 시점을 낸다.

전체 안전 시점은 회복 가능한 조건들의 판정일 중 **이번 달 안에 남은 것 가운데 가장 늦은 날**이다
(팀 확정 2026-09-10: 이미 지난 이번 달 판정은 제외. 그래서 급여통장 시나리오의 c3(10/5)는 빠지고 `safeAfter`는 9/30).
그 날이 지나면 이번 달 판정이 모두 끝난 상태가 된다.

`metric.currentValue`가 있고 `threshold`보다 작으면 그 혜택은 **이미 미적용** 상태다 — 손실 0원으로 두고
안전 시점 계산에서도 제외한다. `evidence` 화면에서 실적 기준을 올리면 이 경로로 모든 화면이 바뀐다.

**금액 (`money.ts`)**
- `effect.kind === "rate_delta"` — 원금 × |금리차| = 연간 손실. 대출은 첫해 기준이므로 그렇게 라벨링한다.
- `effect.kind === "monthly_benefit"` — 월 금액 × 12.
- 절감(`trigger.savings`)은 변경 대상 상품의 `facts`에서 읽는다. 연회비는 `facts.annualFee`, 보험료는 `facts.monthlyPremium × 12`. JSON에 금액을 따로 적지 않는다.
- 모든 금액은 연 단위로 통일한다. 상품마다 만기가 달라 총액으로는 더할 수 없다.
- 순수 함수로 작성한다. 같은 입력에 항상 같은 값이 나와야 한다.

**보류 판정 (`trigger.missing`)**
- 트리거에 `missing: ["새 대출의 적용금리·상환 조건"]`처럼 비교에 빠진 핵심 입력이 적혀 있으면 `verdict.kind === 'pending'`.
  손실 쪽 소계는 그대로 계산하되 라벨이 "확인된 항목의 변화 소계"가 되고, 유불리 결론(keep/switch)을 내지 않는다.
  **반대편 숫자 없이 "유지가 유리"라고 말하지 않는다.** 실행 안내는 빠진 입력을 채우는 단계를 맨 앞에 둔다.
- 절감·후보가 없는 트리거(대출·예금·급여통장)가 여기에 해당한다. 보류를 풀려면 데이터에 그 값을 넣고 `missing`을 비운다.

**실적 확인 상태 (`interpreter.ts` · `metricStatus`)**
- `currentValue`가 없으면 충족으로 계산하되 **가정(`assumed`)이라고 드러낸다.** 빈 값을 조용히 충족으로 처리하지 않는다.
  `verified`(실적 데이터) · `user`(사용자 확인) · `assumed`(값 없음·충족 가정) · `fixed`(PERMANENT, 재판정 없음).
  영향 항목의 펼친 내역에 "실적 확인" 줄로 나온다.

**시점별 손익 (`horizon.ts`)**
- `지금 실행(0개월)` = 절감 − 연간 손실. 바로 실행하면 절감은 생기고 혜택은 없어진다.
- `m개월 유지(m>0)` = 연간 손실 × m/12 − 절감 × ceil(m/12). 절감은 연 단위로 나가는 비용이라 해가 바뀔 때마다 한 번씩 더 든다.
- 구간은 `0·3·6·9·12개월` (팀 확정 2026-09-20). 유지 손익이 개월 수에 비례하므로 1년 넘는 구간은 배수일 뿐이라 두지 않는다.
- 강조 구간: ① 지금 실행이 이미 이득(순손익 ≥ 0)이면 0개월 → ② 회복 불가 조건이 있으면 그 회복 시점을 넘기는 가장 이른 구간 → ③ 그 외에는 순손익이 양(+)으로 돌아서는 가장 이른 구간.
  만기가 1년보다 멀면 마지막 구간으로 떨어지고 `reason`이 `recover-beyond`가 된다.
- **③(`positive`)은 "최적 변경 시점"이 아니라 손익분기다.** 배지도 "추천"이 아니라 "손익분기"로 그린다.
  절감이 없으면 손익분기 자체가 없으므로 강조 막대·배지를 그리지 않는다(`HorizonChart quiet`). 변경 *시점*은 판정일·만기 같은 확인된 사건(타임라인·실행 안내)으로 정한다.
- `horizon.reason`이 문구의 근거다. 차트 아래 문구는 이유를 보고 고른다 — 구간 숫자만 보고 문장을 짓지 않는다.
- 차트 제목은 "유지 기간별 예상 손익 · 지금 {verb} 대비"다. 미래 금리·상품 존속을 예측하지 않는다고 각주에 적는다.
- 막대 높이는 이 값에서만 나온다. 차트에 숫자를 박지 않는다.

**연쇄 (`graph.ts`)**
- `binds.target`이 변경 대상 상품을 가리키는 조건을 전부 찾는다.
- 각 조건의 `binds.holder`가 영향받는 상품이다.
- 관계도의 선은 이 배열을 순회해 그린다. **SVG를 손으로 박아두지 않는다.** 조건이 늘면 선도 늘어야 한다.

**중도해지 이자 (`money.ts` · `earlyTermination`)**
- 정기예금을 만기 전에 깨면 약정이율 대신 `facts.earlyTerminationRate`가 예치기간에 적용된다.
- 만기이자 = 원금 × 약정이율 × (가입일→만기)/365 · 중도해지이자 = 원금 × 중도해지이율 × (가입일→오늘)/365.
- 손실 = 만기이자 − 중도해지이자. **해지 시 한 번 확정되는 금액이라 연 단위 합계(`netAnnual`)에 더하지 않는다.**
  화면에서도 따로 그린다. 같은 축에 올리는 순간 "상품마다 만기가 달라 더할 수 없다"는 규칙이 깨진다.
- 근거(원금·이율·가입일·만기)가 하나라도 없으면 `null`. 값을 지어내지 않는다.

**연계 가입 제안 (`addon.ts`)**
- 갈아타기(`recommend.ts`)와 목적이 다르다. 보유 상품을 **그대로 두고 더하면** 이득인 후보를 찾는다.
- `candidates[].mode`가 `add`인 후보만 본다. 트리거와 무관하다 — 상시 분석의 결과물이라 변경을 전제하지 않는다.
- 연 순이득 = 되살아나는 혜택 + 후보 자체 혜택 − 후보 비용(연회비 + 월 보험료 × 12).
- 되살아나는 혜택 = 지금 실적 미달로 **미적용 중인** 조건 중 후보의 `satisfies.kinds`가 덮는 것들의 연 혜택.
  `evidence`에서 실적 기준을 올려 조건이 꺼지면 이 값이 살아난다 — 상시 분석과 제안이 한 줄로 이어지는 자리다.
- 변경 대상과 **같은 종류**의 후보는 그 트리거 화면에서 뺀다. 바꿀지 보고 있는 상품을 하나 더 만들라고 권하면 화면이 어긋난다.
- 갈아타기 후보(`recommend.ts`)의 `recommended`는 화면에 "이득" 배지로만 쓴다 — "추천"이라 부르지 않는다.
  후보 행마다 **"이 안으로 절차 보기"**, 목록 끝에 **"새 상품 없이 {verb}"** 행이 있고, 여기서 누른 것이 `store.chosen`에 들어가 실행 안내의 기준 안이 된다.
- 제안·추천 후보 행의 **"신청 경로 보기"** 는 `candidates[].contact`를 `ContactSheet`로 펼칠 뿐이다. 실행 안내의 창구 블록과 같은 컴포넌트다.
  **"신청하기" 버튼은 두지 않는다** — 앱이 가입을 처리하는 것처럼 보이는 순간 원칙이 깨진다.

**상시 요약 (`watch.ts`)**
- 트리거 없이 보유 상태만 본다. MAPPED 조건 전부의 판정일·실적 충족 여부를 낸다.
- `dueSoon` = 살아있고 이번 달 안에 판정일이 남은 것, 날짜 빠른 순. `nextDate`는 그 첫 날.
- 홈 배너의 "상품 n개 · 조건 m건 상시 분석 중"도 여기서 온다. 숫자를 화면에 적지 않는다.

**실행 안내 (`actionplan.ts`)**
- **새로 계산하지 않는다.** 안전 시점·회복 시점·갈아타기 결과·중도해지 손실을 받아 순서로 엮을 뿐이다.
- 판정과 무관하게 **해지(변경) 절차만** 낸다. 유지 절차는 안내하지 않는다 — 그대로 두는 데 절차는 없다.
  `keep`이면 머리글에 "확인된 조건에서는 유지가 유리하다"를, `pending`이면 "전체 비교는 보류 중"을 적고 그래도 바꿀 때의 순서를, `switch`면 실행 순서를 낸다. 단계 목록은 셋이 같다.
  대신 결과 상세 화면에 **"유지를 택한다면 지킬 조건"**(`derive().maintain` — 살아있는 조건의 실적 기준·판정 주기)을 둔다. 절차가 아니라 조건 목록이다.
- 순서: **빠진 입력 확인(있으면) → 새 상품의 발급 가능 여부·우대 인정 조건 확인 → 자동납부·결제계좌 이전과 인정 시점 확인 → 이번 달 판정 종료까지 대기 → 중도해지·비가역 손실 경고 → 기존 상품 정리 신청.**
  미확인·비가역 손실 경고는 해지 단계보다 **앞에** 온다.
- 문구: "먼저 만듭니다"가 아니라 "발급 가능 여부와 우대 인정 조건을 확인합니다", "실적을 옮깁니다"가 아니라 "자동납부·결제계좌를 옮기고 인정 시점을 확인합니다",
  "10월 1일부터 안전"이 아니라 "10월 1일부터 · 이번 달 판정 종료 후" + 그 뒤 확인할 항목(`dueThisMonth`). "안전"이라는 말을 기한에 쓰지 않는다.
- 연락처는 `products[].contact` · `candidates[].contact`에서만 온다. **가상 브랜드이므로 전화번호를 지어내지 않는다.**
  `tel`이 비어 있으면 화면이 "공식 앱·홈페이지에서 확인"으로 그리고, 안내문에 그 사실을 적는다.
  대신 담당 부서·신청 경로·운영 시간·창구에서 물어볼 것을 데이터로 채운다. 창구 시트 맨 아래에 "시연용 샘플 창구 정보"라고 적는다 — 부서·시간도 샘플이다.

**멀티 트리거**
- `scenario.triggers[]`가 분석 진입점이다. `derive(scenario, triggerId)`로 트리거마다 독립된 결과가 나온다.
- 트리거를 추가하려면 `triggers`에 한 줄, 그 상품을 `binds.target`으로 가리키는 조건 몇 개, `expected.triggers`에 기대치를 넣으면 화면은 손대지 않아도 된다.

## 출처 태그 — 반드시 지킬 것

| 태그 | 색 | 무엇 |
|---|---|---|
| 약관에서 추출 | `#5B4BB8` / 배경 `#EDE9FB` | 조건의 `expr`, `binds`, `effect` |
| 계산 결과 | (그리지 않음) | 판정일, 손실 금액, 안전 시점, 시점별 손익 — 이 앱의 기본값이라 **태그 없음 = 계산 결과** |
| 보유 상품 정보 | `#6B7280` / 배경 `#EDEFF2` | `products[].facts` |
| 사용자 확인 | `#B45309` / 배경 `#FBEEDC` | 사용자가 수정한 값 |

태그는 **값과 같은 객체에 담는다** (`Tagged<T> = { value, source }`). 화면에서 조건문으로 붙이지 않는다.
값을 만드는 함수가 출처를 함께 반환하는 구조로 만든다.

**노출 위치** — 메인 흐름(홈·영향 리스트 상단·최종 판단 헤드라인)은 시안대로 깔끔하게 두고,
태그는 상세에서 보여준다: 영향 항목의 펼친 내역, 체크리스트, 합계 근거 줄, 타임라인, 관계도, 근거 화면.
`Amount` 컴포넌트의 `showTag` 로 켠다. `Tagged<T>` 구조 자체는 어디서도 벗기지 않는다.

## 절대 하지 말 것

- 금액이나 날짜를 문자열로 하드코딩하지 않는다. `194,000`이나 `2026-09-30`을 JSX에 직접 쓰면 수정 기능이 무의미해진다. `scenario.json`의 `expected` 블록은 테스트용이지 표시용이 아니다.
- 상품명이 들어가는 문장에 조사를 붙여 쓰지 않는다. `format.ts`의 `withJosa(name, '을/를')`을 쓴다. pair는 **"받침 있을 때/없을 때"** 순서다 (`'과/와'`, `'을/를'`, `'은/는'`).
- `new Date()`를 호출하지 않는다.
- 서비스명·상품명·금융사 연락처를 화면 코드에 적지 않는다. 전부 `scenario.json`에서 읽는다.
- 해지·가입이 **실행된 것처럼** 보이는 문구를 쓰지 않는다. 이 앱은 계산하고 안내할 뿐 계약을 처리하지 않는다.
- 중도해지 이자처럼 일회성인 금액을 연 단위 합계에 더하지 않는다.
- localStorage, sessionStorage를 쓰지 않는다.
- 화면이 각자 값을 복사해 갖지 않는다. 단일 상태 객체를 공유하고 `derive()` 결과만 읽는다.
- 핵심 입력이 빠졌는데(`trigger.missing`) 유불리 결론을 내지 않는다. 소계와 "보류"만 낸다.
- 앱이 고른 후보(`best`)를 기준으로 실행 안내를 짜지 않는다. 사용자가 고른 `chosen`만 따른다.
- 첫 양수 구간을 "추천 시점"이라 부르지 않는다. 손익분기다.
- 허브에 분석 전 손익 금액을 적지 않는다.
- 분석 연출에서 실시간 약관 추출·AI 호출이 있는 것처럼 쓰지 않는다.
